node_modules/

# Working screenshots — never part of the site.
*.png
!images/*.png

# Local working files, kept off the public site.
*.md
*.sql
*.zip

# Holds Asem's own messages. Must never reach the remote.
areket.html
